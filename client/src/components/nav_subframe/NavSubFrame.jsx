import React from 'react'
import { NavSubFrameContainer } from './styles'

export const NavSubFrame = ({children}) => {
  return (
    <NavSubFrameContainer>
        {children}
    </NavSubFrameContainer>
  )
}
